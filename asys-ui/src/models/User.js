export default class User {
  constructor(firstName, lastName, emailAddress, account) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.emailAddress = emailAddress;
    this.account = account;
  }
}
